# 🏗 Feature-Sliced Design Architecture

Этот проект использует архитектуру **Feature-Sliced Design (FSD)** - современную методологию организации frontend проектов.

## 📚 Оглавление

- [Что такое FSD?](#что-такое-fsd)
- [Структура проекта](#структура-проекта)
- [Слои архитектуры](#слои-архитектуры)
- [Правила импортов](#правила-импортов)
- [Best Practices](#best-practices)
- [Примеры](#примеры)

## 🎯 Что такое FSD?

**Feature-Sliced Design** - это архитектурная методология для frontend проектов, которая:

- ✅ **Масштабируется** - легко добавлять новые фичи
- ✅ **Понятна** - структура отражает предметную область
- ✅ **Модульна** - код разбит на независимые модули
- ✅ **Тестируема** - легко писать и поддерживать тесты

## 📁 Структура проекта

```
src/
├── app/                    # 🚀 Инициализация приложения
│   ├── providers/         # React провайдеры
│   ├── styles/            # Глобальные стили
│   └── README.md          # Документация слоя
│
├── processes/             # 🔄 Сложные межстраничные процессы (опционально)
│   └── README.md
│
├── pages/                 # 📄 Страницы приложения
│   ├── home/             # Главная страница
│   ├── catalog/          # Страница каталога
│   └── README.md
│
├── widgets/               # 🧩 Крупные блоки интерфейса
│   ├── header/           # Шапка сайта
│   ├── product-card/     # Карточка товара
│   └── README.md
│
├── features/              # ⚡️ Функциональность (действия пользователя)
│   ├── add-to-cart/      # Добавить в корзину
│   ├── auth/             # Авторизация
│   └── README.md
│
├── entities/              # 📦 Бизнес-сущности
│   ├── product/          # Товар
│   ├── user/             # Пользователь
│   ├── cart/             # Корзина
│   └── README.md
│
└── shared/                # 🔧 Переиспользуемый код
    ├── ui/               # UI Kit
    ├── lib/              # Утилиты и хуки
    ├── api/              # API клиент
    ├── config/           # Конфигурация
    ├── types/            # Общие типы
    ├── assets/           # Статические файлы
    └── README.md
```

## 🎨 Слои архитектуры

### 1. 🚀 App - Инициализация приложения

**Отвечает за:** Настройка провайдеров, роутинг, глобальные стили

```tsx
// app/App.tsx
import { AppProviders } from './providers';
import { AppRouter } from './router';

export const App = () => (
  <AppProviders>
    <AppRouter />
  </AppProviders>
);
```

📖 [Подробная документация](./app/README.md)

---

### 2. 🔄 Processes - Межстраничные процессы (опционально)

**Отвечает за:** Сложные пользовательские сценарии (онбординг, чекаут)

> ⚠️ **Внимание:** Большинству проектов этот слой не нужен!

📖 [Подробная документация](./processes/README.md)

---

### 3. 📄 Pages - Страницы

**Отвечает за:** Композиция виджетов и фич для конкретного маршрута

```tsx
// pages/home/ui/HomePage.tsx
export const HomePage = () => (
  <div>
    <Header />
    <CategoryNav />
    <ProductList />
    <Footer />
  </div>
);
```

📖 [Подробная документация](./pages/README.md)

---

### 4. 🧩 Widgets - Крупные блоки

**Отвечает за:** Композитные UI блоки (header, product-card, cart-summary)

```tsx
// widgets/product-card/ui/ProductCard.tsx
export const ProductCard = ({ product }) => (
  <article>
    <img src={product.image} />
    <h3>{product.name}</h3>
    <Price value={product.price} />
    <AddToCartButton productId={product.id} />
  </article>
);
```

📖 [Подробная документация](./widgets/README.md)

---

### 5. ⚡️ Features - Функциональность

**Отвечает за:** Действия пользователя (добавить в корзину, авторизоваться)

```tsx
// features/add-to-cart/ui/AddToCartButton.tsx
export const AddToCartButton = ({ productId }) => {
  const { addToCart, isLoading } = useAddToCart();
  
  return (
    <Button onClick={() => addToCart(productId)} loading={isLoading}>
      В корзину
    </Button>
  );
};
```

📖 [Подробная документация](./features/README.md)

---

### 6. 📦 Entities - Бизнес-сущности

**Отвечает за:** Данные и бизнес-модели (product, user, cart)

```ts
// entities/product/model/hooks.ts
export const useProduct = (id: string) => {
  return useQuery({
    queryKey: ['product', id],
    queryFn: () => productApi.getProduct(id),
  });
};
```

📖 [Подробная документация](./entities/README.md)

---

### 7. 🔧 Shared - Общий код

**Отвечает за:** Переиспользуемые компоненты, утилиты, конфигурация

```tsx
// shared/ui/button/Button.tsx
export const Button = ({ children, ...props }) => (
  <button className={styles.button} {...props}>
    {children}
  </button>
);
```

📖 [Подробная документация](./shared/README.md)

---

## 🔗 Правила импортов

FSD использует **однонаправленный поток зависимостей**:

```
app → processes → pages → widgets → features → entities → shared
```

### ✅ Разрешенные импорты:

```tsx
// ✅ Страница может импортировать виджеты
import { Header } from '@/widgets/header';

// ✅ Виджет может импортировать фичи
import { AddToCartButton } from '@/features/add-to-cart';

// ✅ Фича может импортировать сущности
import { useProduct } from '@/entities/product';

// ✅ Все могут импортировать shared
import { Button } from '@/shared/ui/button';
```

### ❌ Запрещенные импорты:

```tsx
// ❌ Сущность НЕ может импортировать фичу
import { AddToCart } from '@/features/add-to-cart'; // WRONG!

// ❌ Фича НЕ может импортировать другую фичу
import { Auth } from '@/features/auth'; // WRONG!

// ❌ Виджет НЕ может импортировать страницу
import { HomePage } from '@/pages/home'; // WRONG!
```

## 🎓 Best Practices

### 1. Public API

Каждый модуль должен экспортировать только необходимое через `index.ts`:

```ts
// features/add-to-cart/index.ts
export { AddToCartButton } from './ui/AddToCartButton';
export { useAddToCart } from './model/useAddToCart';

// ❌ Не экспортируем внутренние детали
// export { addToCartApi } from './api/addToCartApi';
```

### 2. Именование

- **Слои/папки:** `kebab-case` (add-to-cart, product-card)
- **Компоненты:** `PascalCase` (AddToCartButton, ProductCard)
- **Хуки:** `use` + имя (useAddToCart, useProduct)
- **Типы:** `PascalCase` (Product, User, CartItem)

### 3. Один модуль = одна ответственность

```
features/
├── add-to-cart/        # ✅ Одна фича - добавление в корзину
├── remove-from-cart/   # ✅ Отдельная фича - удаление из корзины
└── cart-actions/       # ❌ Слишком абстрактно
```

### 4. Композиция над наследованием

```tsx
// ✅ Хорошо - композиция готовых блоков
export const ProductCard = ({ product }) => (
  <article>
    <ProductImage src={product.image} />
    <ProductInfo product={product} />
    <AddToCartButton productId={product.id} />
  </article>
);

// ❌ Плохо - все в одном компоненте
export const ProductCard = ({ product }) => {
  // 200 строк кода...
};
```

## 📚 Дополнительные ресурсы

- 📖 [Официальная документация FSD](https://feature-sliced.design/)
- 🎥 [Видео-туториалы](https://www.youtube.com/playlist?list=PLYyPcR0PAuL6-p84SMk9kqEJp3k9xKqYZ)
- 💬 [Telegram чат](https://t.me/feature_sliced)

## 🚀 Начало работы

1. **Изучите документацию** каждого слоя (README.md в папках)
2. **Посмотрите примеры** в документации
3. **Начните с simple** - создайте простую страницу
4. **Следуйте правилам** импортов и именования

---

🎉 **Успешной разработки!** Если возникнут вопросы - обратитесь к документации в каждом слое.
