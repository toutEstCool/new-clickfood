# 🍔 ClickFood

Приложение для заказа еды, построенное на архитектуре **Feature-Sliced Design**.

## 🚀 Технологии

- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **CSS Modules** - Стилизация компонентов
- **Axios** - HTTP клиент
- **React Compiler** - Оптимизация производительности

## 📁 Архитектура

Проект использует **Feature-Sliced Design (FSD)** - современную методологию организации frontend кода.

```
src/
├── app/         # 🚀 Инициализация приложения
├── processes/   # 🔄 Межстраничные процессы
├── pages/       # 📄 Страницы
├── widgets/     # 🧩 Крупные блоки UI
├── features/    # ⚡️ Бизнес-функции
├── entities/    # 📦 Бизнес-сущности
└── shared/      # 🔧 Переиспользуемый код
```

📖 **[Подробная документация по архитектуре](./src/README.md)**

## 🎯 Быстрый старт

### 1. Установка зависимостей

```bash
pnpm install
```

### 2. Запуск dev-сервера

```bash
pnpm dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

### 3. Сборка для production

```bash
pnpm build
```

### 4. Предпросмотр production сборки

```bash
pnpm preview
```

## 📚 Документация

Каждый слой архитектуры имеет свою документацию с примерами:

- 📖 [App Layer](./src/app/README.md) - Инициализация приложения
- 📖 [Processes Layer](./src/processes/README.md) - Межстраничные процессы
- 📖 [Pages Layer](./src/pages/README.md) - Страницы приложения
- 📖 [Widgets Layer](./src/widgets/README.md) - Крупные UI блоки
- 📖 [Features Layer](./src/features/README.md) - Бизнес-функциональность
- 📖 [Entities Layer](./src/entities/README.md) - Бизнес-сущности
- 📖 [Shared Layer](./src/shared/README.md) - Общий код

## 🎨 Примеры компонентов

В проекте уже созданы примеры для быстрого старта:

### UI Компонент (shared)
```tsx
import { Button } from '@/shared/ui/button';

<Button variant="primary" size="md">
  Нажми меня
</Button>
```

### Хуки (shared)
```tsx
import { useDebounce } from '@/shared/lib/hooks/useDebounce';

const debouncedValue = useDebounce(search, 500);
```

### Утилиты (shared)
```tsx
import { formatPrice } from '@/shared/lib/utils/format';

const price = formatPrice(1500); // "1 500 ₽"
```

## 🛠 Path Aliases

Проект настроен с удобными алиасами для импортов:

```tsx
// ✅ Используйте алиасы
import { Button } from '@/shared/ui/button';
import { useProduct } from '@/entities/product';
import { AddToCart } from '@/features/add-to-cart';

// ❌ Вместо относительных путей
import { Button } from '../../../shared/ui/button';
```

Доступные алиасы:
- `@/app/*` - Слой приложения
- `@/processes/*` - Процессы
- `@/pages/*` - Страницы
- `@/widgets/*` - Виджеты
- `@/features/*` - Фичи
- `@/entities/*` - Сущности
- `@/shared/*` - Общий код

## 📝 Правила разработки

### 1. Правило импортов

Соблюдайте иерархию зависимостей:

```
app → processes → pages → widgets → features → entities → shared
```

✅ **Можно:**
```tsx
// Страница импортирует виджеты
import { Header } from '@/widgets/header';

// Виджет импортирует фичи
import { AddToCart } from '@/features/add-to-cart';

// Все могут импортировать shared
import { Button } from '@/shared/ui/button';
```

❌ **Нельзя:**
```tsx
// Сущность импортирует фичу
import { AddToCart } from '@/features/add-to-cart'; // WRONG!

// Фича импортирует другую фичу
import { Auth } from '@/features/auth'; // WRONG!
```

### 2. Public API

Каждый модуль экспортирует только необходимое через `index.ts`:

```tsx
// features/add-to-cart/index.ts
export { AddToCartButton } from './ui/AddToCartButton';
export { useAddToCart } from './model/useAddToCart';
```

### 3. Именование

- **Папки:** `kebab-case` (add-to-cart, product-card)
- **Компоненты:** `PascalCase` (AddToCartButton, ProductCard)
- **Хуки:** `use` + имя (useAddToCart, useProduct)
- **Типы:** `PascalCase` (Product, User)

## 🎓 Ресурсы по FSD

- 📖 [Официальная документация](https://feature-sliced.design/)
- 🎥 [Видео-курс](https://www.youtube.com/playlist?list=PLYyPcR0PAuL6-p84SMk9kqEJp3k9xKqYZ)
- 💬 [Telegram чат](https://t.me/feature_sliced)

## 🤝 Разработка

Проект готов к разработке! Начните с изучения документации в папке `src/README.md`.

### Рекомендуемый порядок:

1. Создайте базовые **entities** (Product, User, Cart)
2. Добавьте **features** (авторизация, добавление в корзину)
3. Соберите **widgets** из фич и сущностей
4. Составьте **pages** из виджетов
5. Настройте **app** (провайдеры, роутинг)

---

Сделано с ❤️ с использованием Feature-Sliced Design
