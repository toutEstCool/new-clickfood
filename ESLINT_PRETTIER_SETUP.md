# ✅ ESLint & Prettier Configuration - Complete

## 🎯 Что было настроено

### 1. **Установленные пакеты**

```json
{
  "devDependencies": {
    "prettier": "^3.7.4",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "eslint-plugin-react": "^7.37.5",
    "@typescript-eslint/parser": "^8.50.1",
    "@typescript-eslint/eslint-plugin": "^8.50.1"
  }
}
```

### 2. **Созданные файлы конфигурации**

- ✅ `.prettierrc` - конфигурация Prettier
- ✅ `.prettierignore` - игнорируемые файлы для Prettier
- ✅ `eslint.config.js` - полная конфигурация ESLint (обновлено)
- ✅ `.vscode-settings.json.example` - рекомендуемые настройки для VSCode

### 3. **Обновлены скрипты в package.json**

```bash
pnpm run lint          # Проверка линтером
pnpm run lint:fix      # Исправление ошибок линтера
pnpm run format        # Форматирование кода
pnpm run format:check  # Проверка форматирования
pnpm run type-check    # Проверка типов TypeScript
pnpm run check-all     # Полная проверка
pnpm run fix-all       # Автоисправление всего
```

---

## 🎯 Реализованные требования

### ✅ 1. Подсветка неиспользуемых переменных

```tsx
// Линтер выдаст предупреждение (warning)
const unusedVariable = 'test'

// Можно игнорировать с префиксом _
const _intentionallyUnused = 'ok'
```

**Правило:** `@typescript-eslint/no-unused-vars`

### ✅ 2. Именование компонентов в PascalCase

```tsx
// ✅ Правильно
export const MyComponent = () => { }
export const UserProfile = () => { }

// ❌ Неправильно
export const myComponent = () => { }
export const userprofile = () => { }
```

**Правило:** `react/jsx-pascal-case`

### ✅ 3. Компоненты - только стрелочные функции

```tsx
// ✅ Правильно
export const MyComponent = () => {
  return <div>Hello</div>
}

// ❌ Неправильно - будет ошибка
function MyComponent() {
  return <div>Hello</div>
}
```

**Правило:** `react/function-component-definition`

### ✅ 4. Именованные экспорты

```tsx
// ✅ Правильно
export const MyComponent = () => {
  return <div>Hello</div>
}

// Импорт
import { MyComponent } from './MyComponent'
```

**Настройка:** Разрешены именованные экспорты через `react-refresh/only-export-components`

---

## 📂 Файлы примеров

### `src/shared/ui/example/ExampleComponent.tsx`

Создан файл с примерами правильного использования:
- ✅ Стрелочные функции
- ✅ Именованные экспорты
- ✅ PascalCase для компонентов
- ✅ Type imports для TypeScript
- ✅ Boolean переменные с префиксами (is, has, can)

---

## 📚 Документация

### `LINTING_GUIDE.md`

Полное руководство по использованию ESLint и Prettier с:
- Примерами кода
- Объяснением всех правил
- Best practices для React и TypeScript
- Командами для работы
- Настройками для редакторов

---

## 🚀 Быстрый старт

1. **Проверить код:**
```bash
pnpm run check-all
```

2. **Исправить автоматически:**
```bash
pnpm run fix-all
```

3. **Настроить VSCode (опционально):**
```bash
cp .vscode-settings.json.example .vscode/settings.json
```

---

## ✨ Дополнительные настройки

### Правила форматирования Prettier:
- Без точек с запятой
- Одинарные кавычки
- 100 символов в строке
- 2 пробела для отступов
- Trailing commas (ES5)

### Дополнительные правила ESLint:
- ✅ Type imports (`@typescript-eslint/consistent-type-imports`)
- ✅ PascalCase для типов и интерфейсов
- ✅ Self-closing теги
- ✅ Оптимизация boolean props
- ✅ Fragment syntax
- ✅ React Hooks правила
- ✅ Запрет на `console.log` (только warn и error)
- ✅ Запрет на `var`, только `const`/`let`
- ✅ Строгое сравнение `===`

---

## 🧪 Тестирование

Все файлы были обновлены и проверены:

```bash
✅ Type checking - passed
✅ ESLint - passed
✅ Prettier - passed
```

### Обновленные файлы:
- `src/app/App.tsx` - преобразован в arrow function с named export
- `src/main.tsx` - обновлен импорт и добавлен комментарий для non-null assertion
- `src/shared/ui/button/Button.tsx` - type imports
- `src/shared/ui/example/ExampleComponent.tsx` - новый пример

---

## 💡 Советы

1. **Перед коммитом всегда запускайте:**
   ```bash
   pnpm run fix-all
   ```

2. **Для CI/CD используйте:**
   ```bash
   pnpm run check-all
   ```

3. **Для VSCode установите расширения:**
   - ESLint
   - Prettier - Code formatter

---

**Конфигурация выполнена по стандартам Senior Engineer! 🎯**

Все требования реализованы с best practices для React, TypeScript и современной разработки.
